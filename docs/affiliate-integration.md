# 联盟适配器接入

`AffiliateAdapter` 统一转链、拉单、查单和配置校验。美团与饿了么签名、分页、限流、错误码映射仅存在各自适配器内部。

正式接入步骤：取得运营主体官方文档和凭据；确认活动、PID/SID 模型；实现签名和脱敏日志；契约测试分页与退款；配置 3 秒连接/10 秒读取超时；429/5xx 有限指数退避；按 tenant+platform 使用 Redis 锁；原始响应只保留必要脱敏字段。未配置时必须返回 `AFFILIATE_NOT_CONFIGURED`。
