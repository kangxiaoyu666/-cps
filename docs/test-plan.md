# 测试计划

1. 身份：伪造 openid 无效；令牌过期、撤销、用户禁用。
2. 租户：A 管理员不能访问 B 的用户、订单、钱包和提现。
3. 拉单：重复订单 upsert；超时/限流不落错误资金数据；同租户平台锁互斥。
4. 结算：重复和并发只成功一次；规则修改不影响快照；退款生成冲正。
5. 提现：门槛/余额/幂等；并发审核与重复付款；拒绝取消释放冻结。
6. 页面：首页、订单、钱包的加载、空、错误、正常状态。
7. 灾备：备份在独立环境恢复并通过账务对账。

核心领域单元测试覆盖目标 80%，资金路径增加 MySQL 集成测试。
