{
  "name": "waimai-cps-monorepo",
  "private": true,
  "scripts": {
    "lint": "npm --prefix admin-web run lint",
    "build": "npm --prefix admin-web run build",
    "typecheck": "npm --prefix admin-web run typecheck"
  }
}
